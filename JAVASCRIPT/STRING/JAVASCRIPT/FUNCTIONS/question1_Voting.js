// Write a function that takes person age as a number as input and determines, person are eligible or not for voting
function voting(age) {
    if (age >= 18) {
        return "You are eligible to vote.";
    } else {
        return "You are not eligible to vote.";
    }
}

console.log(voting(20));Get-ChildItem -Recurse

